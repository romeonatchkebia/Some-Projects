
//სანტიმეტრების, მეტრებში გადამყვანი კონვერტორი

/* function cmToM(cm) {
    let x = cm / 100;
    let y = cm;
    console.log(`${y} სანტიმეტრი არის ${x} მეტრი`);
}

cmToM(400); */

/* function sumCalculator(x) {
    let sum = 0;
    
    for (let i = 0; i < x.length; i++) {
        sum += x[i];
    }
    let sashualo = sum / x.length;
    
    return sashualo;
}

console.log(sumCalculator([4, 11, 42, 72, 25, 7, 8]));  */

